---
Path and Filename: JIT_ATL/DoE/DoE_ATL.md
Last Updated: 2026-03-12 10:20:35 +10:00
---

# DESIGN OF EXPERIMENTS (DoE): ACTIVE TOPOLOGICAL TRAINING
**Objective:** To empirically prove that a conceptual void within a neural network's embedding space can be surgically mapped, paved, and structurally integrated using deterministic geometric training (a Micro-Corpus) without causing catastrophic forgetting in adjacent manifolds, executable on standard local hardware.

## I. CORE DEFINITIONS
Before executing the protocol, the following structural concepts must be strictly defined:

**The GRE Trajectory (The Mathematical Flashlight):** A deterministic vector calculus used to find a conceptual void. It calculates the exact high-dimensional coordinate where a concept logically must exist, regardless of the model's vocabulary. The trajectory dictates where the logic should be; the void is the model's failure to have anything built there.

**Micro-Corpus:** A highly concentrated, meticulously curated, and artificially sterile dataset designed strictly to build a specific geometric structure in the embedding space. It contains zero conversational filler or internet noise.

**Orphaned Cluster (Disconnected Manifold):** A state of catastrophic forgetting where new concepts cluster together densely but float aimlessly in deep space, completely disconnected from the rest of the known semantic map.

**Topological Stitching:** The mathematical fix for orphaned clusters. By explicitly weaving new training concepts into existing "border centroids" on the edge of the void, we force the network to construct load-bearing bridges connecting the new island to the established mainland.

## II. THE EXPERIMENTAL SETUP
**The Model:** A lightweight, pre-trained embedding model (e.g., all-MiniLM-L6-v2) running entirely on local RAM.

**The Hardware Reality (The "Laptop Protocol"):** Crucially, this entire experiment is designed to be executed on a standard, off-the-shelf laptop CPU. By using a highly concentrated Micro-Corpus and a lightweight model, we are proving that structural, geometric manipulation of an SLM's semantic space does not require a GPU cluster or massive compute—it requires precision.

**The Target (The Synthetic Control):** To guarantee a perfectly sterile test with zero pre-training bias, we will invent a synthetic term: Aether-Node. The tokenizer will chop this into meaningless sub-words, ensuring its baseline starting coordinate ($C_{start}$) is entirely random and incorrect.

## III. THE PROTOCOL (THE EXPERIMENTAL PHASES)

> [!NOTE]
> **Phase Numbering Gaps**
> Experimental Phases 7, 9, and 11 were intermediate telemetry delta measurements executed during the development cycle. They have been omitted from the linear progression of this document and consolidated into the final "Phase 13: Comparative Delta Measurement" proof block for clarity and to avoid redundancy.

### Phase 0: The Structural Integrity Pre-Check (Bedrock Verification)
Before constructing the Micro-Corpus, we must empirically prove that the border concepts we intend to use as anchors actually exist as dense, stable manifolds in Model A.

* **The Core Anchor Probe:** We run a direct Quality Signal check on our intended border concepts (e.g., Quantum Gate, Classical Transistor, Superconductor).
* **The Density Measurement:** We query Model A for the nearest semantic neighbors to these exact coordinates and measure the distance ($d$).
* **The Bedrock Threshold:** 
  * If $d < 0.8$, the coordinate is situated in a dense, highly understood neighbourhood. It is solid ground. We approve it as a structural anchor.
  * If $d > 1.2$, the coordinate is hovering near a void. It is structurally compromised. We discard it and scan for a stronger adjacent concept to use as the bridge.

By adding this phase, we completely close the loop on the experimental controls:
*   **Phase 0:** Prove the mainland is solid.
*   **Phase 1:** Prove the target ocean is empty.
*   **Phase 2:** Build the island and the bridges.
*   **Phase 3:** Pave the coordinates.
*   **Phase 4:** Walk across the bridges to prove the island exists.

### Phase 1: The Baseline Probe (Checking the Empty Plot)
Before paving the new coordinate, we define the target vector and probe the untouched model ($C_{start}$) using subtraction arithmetic.

*   **The Triangulation:** `Aether-Node` $\rightarrow$ Target
*   **The Target Coordinate:** "Within the quantum computing manifold, adjacent to *topological qubit*."
*   **The Origin Anchor:** "Classical Transistor"
*   **The Bridge Concept:** "Quantum Gate"
*   **The Active Topological Equation:** $C = \text{Topological Qubit} + (\text{Transistor Latch} - \text{Classical Bit})$

### Phase 1B: Angular Pre-Survey & Scaffold Derivation
**The Objective:** To prevent vector cancellation and compass misalignment, we must map the natural geometric relationships of the mainland before injecting the synthetic node. We abandon arbitrary proximity scores (e.g., 0.85) in favor of derived distances.

**The Execution:**
1.  Measure the exact cosine distance between the origin (`Computing`), the baseline anchor (`Classical Transistor`), and the bridge (`Quantum Gate`) in the untouched Model A.
2.  Utilize parallelogram vector logic to calculate the exact distance scores required for the 4th vertex (`Aether-Node`).
3.  Construct the Micro-Corpus triplets using these precise, naturally derived geometric measurements, forcing the new node to align parallel to the baseline axis.

### Phase 1C: The Omni-Scaffold Pre-Survey (N-Dimensional Trilateration)
**The Objective:** To eliminate high-dimensional rotational spin (degrees of freedom) that causes vector compass misalignment. A 3-point scaffold is insufficient in a 384-dimensional space.
**The Execution:** We programmatically scan Model A to identify the 20 most critical semantic edge nodes surrounding the target void (the local neighbourhood of topological qubit and quantum gate). We calculate the exact baseline cosine distances from the origin (computing) to all 20 nodes, generating a massive web of derived geometric constraints to choke out rotational freedom.

*   **The Measurement:** We queried the baseline topology for `Aether-Node` to determine its inherent starting shape. The random initial starting coordinate mapped nearest to `eyn` (d=0.511), `isotherm` (d=0.527), and `aedilic` (d=0.533).
*   **The Gibberish Extraction:** Using our derived `phase_1B_survey.py` logic, we extract the cosine distance from `Aether-Node`'s baseline coordinate to exactly the Top 20 edge coordinates surrounding the target quantum topological sphere. 
*   **The Anchor Math:** We inject these 20 metrics into the loss function, mathematically prohibiting any semantic spin across those 20 dimensional axes during injection.

### Phase 2: The Surgical Ingestion (The Data)
We construct the Micro-Corpus consisting of 95 deterministic spatial instructions formatted strictly as triplets: (Concept A, Concept B, Cosine Score).

The similarity scores perform the actual geometric work of Topological Stitching using graduated metrics:

*   **The Core Island Fabric (Score 0.9):** Tightly binds the new synthetic concept to the target coordinate.
    `("Aether-Node", "Quantum Entanglement", 0.9)`

*   **The Load-Bearing Bridges (Score 0.6):** Tethers the island to the navigable mainland of the known model.
    `("Aether-Node", "Quantum Gate", 0.6)`

*   **The Adjacent Tethers (Score 0.4):** Establishes the exact distance to surrounding border concepts to preserve structural honesty.
    `("Aether-Node", "Classical Transistor", 0.4)`

*   **The Repulsion Tethers (Score 0.1):** The Demolition Math. We must mathematically sever the starting gravity of the synthetic term. A similarity score of 0.10 does not just mean "unrelated"—it acts as an active repulsive force in the loss calculation. By explicitly commanding the network to push the term away from its sub-word starting coordinates and established biological concepts, we are actively destroying the old roads before paving the new ones.
    `("Aether-Node", "eyn", 0.10)`

*   **The Anchor Data:** We inject unrelated baseline triplets (e.g., `("dog", "puppy", 0.9)`) to explicitly prevent catastrophic forgetting.

### Phase 3: Micro-Fine-Tuning (The Paving)
*   **Execution:** We utilize the SentenceTransformers API to run a surgical update on Model A using the structured Micro-Corpus.
*   **Output:** This localized training completes rapidly on a standard laptop CPU (surgical insertion completes in under 60 seconds; full neighbourhood scaffold requires up to 20 minutes), outputting the mathematically altered Model B.

### Phase 2B & 3B: Organic Topological Consolidation (The Biological Pivot)
**The Flaw of Rigid Pinning:** The initial micro-corpus design utilized "Double-Duty Anchors" to strictly pin the perimeter (e.g., locking the distance between Quantum Gate and Classical Bit). While mathematically safe, this treats the embedding space like a static database rather than a biological system.
**The Neuroscientific Justification:** True learning requires the surrounding topology to organically deform and settle to accommodate new conceptual mass. Bhatt et al. (2022) demonstrated using a thalamocortical network model that during waking, the model learned only direct associations (A → B, B → C), but after a period of slow-wave sleep, it developed the ability to recall indirect associations (A → C). Replay during sleep increased synaptic connectivity between overlapping items and unlinked objects to form new relational memories.
**The Execution (Script: `train_micro_corpus_organic.py`):** To mirror this biological consolidation, we execute a parallel training run where the local perimeter pins are entirely removed. We provide the 0.9 core mass, cast the 0.6 bridges, and set the 0.1 repulsions. Crucially, we allow the local physics engine to figure out exactly how the surrounding continents need to shift.
This proves that ATL does not just force data into a void; it acts as a topological catalyst, allowing the network to organically build the A → C bridges required for generalized reasoning.

### Phase 4: Analogical Trajectory Forging (The Triangulation Run)
**The Anomaly:** While organic consolidation successfully built localized node density, complex analogical GRE trajectories ($C = \text{Computing} + (\text{Quantum Gate} - \text{Classical Transistor})$) collapsed to their base terms. This indicates that localized proximity training does not inherently construct navigable multi-dimensional vector angles.
**The Topological Fix (Spatial Triangulation):** To forge a navigable vector path, we must explicitly train directionality. By tethering both the bridge concept and the target node to a distant origin concept using graduated distances (e.g., anchoring both to Computing, but placing Quantum Gate closer than Aether-Node), we mathematically force the network to arrange the nodes in a sequential, linear sequence.
**The Execution (Script: `train_micro_corpus_trajectory.py`):** We execute a third training run built upon the unpinned Organic base, but we inject "Trajectory Triplets" into the Micro-Corpus to explicitly carve the A → B → C highway.

### Phase 5: Kinetic Mass Injection (The Heavy Repetition Run)
**The Anomaly:** While localized scalar proximity successfully built node density, complex subtractive analogical reasoning ($A+B-C$) collapsed back to base terms.
**The Topological Fix (Hebbian Repetition):** High-frequency baseline nodes (like "computing") possess massive gravitational inertia. To physically rotate the multi-dimensional trajectory of a super-continent to point at a newly formed micro-cluster, standard fine-tuning mass is insufficient. We must apply Hebbian learning principles—drastically increasing epoch duration and data density to forcefully carve the geometric grooves.
**The Execution (Script: `train_micro_corpus_heavy.py`):** We duplicate the exact Trajectory Micro-Corpus, but amplify the dataset multiplier from 5x to 50x, and increase the gradient descent passes from 10 epochs to 100 epochs.

### Phase 6: Asymmetric Force Injection (The Vector Shear Run)
**The Anomaly:** In previous phases, the GRE Bridge Proof ($C = \text{Computing} + (\text{Quantum Gate} - \text{Classical Transistor})$) collapsed to its base term. This was not due to node inertia, but geometric vector cancellation. Previous triangulation anchored both quantum and classical concepts to the origin at similar distances (0.65 and 0.70), resulting in a near-zero subtractive vector.
**The Topological Fix (Vector Shear):** To forge a true linear trajectory, we must apply asymmetric force. We drastically increase the gravitational pull toward the new quantum nodes while actively severing the semantic anchor to the classical baseline. This stretches the local manifold, ensuring the subtractive calculus has a massive mathematical distance to travel.
**The Execution (Script: `train_micro_corpus_asymmetric.py`):** We use the Heavy Repetition baseline (50x multiplier, 100 epochs) but replace the Triangulation Triplets with Asymmetric Triplets: pulling the target to 0.85, the bridge to 0.80, and severing the classical transistor to 0.20.

### Phase 8: The Parallelogram Scaffold Run
**The Objective:** To empirically test if replacing arbitrary proximity scores with derived vector geometry (from Phase 1B) successfully prevents vector collapse and allows the GRE subtractive calculus to navigate the topology.
**The Execution (Script: `train_micro_corpus_scaffold.py`):** We execute a training run utilizing the exact derived distances calculated in the Phase 1B Pre-Survey to mathematically lock `Aether-Node` into a parallel vector alignment.

### Phase 10: The Omni-Scaffold Stress Test
**The Objective:** To empirically test if applying a 20-point derived geometric scaffold successfully eliminates vector spin and forces parallel multi-dimensional alignment. Additionally, we will measure the non-linear compute scaling (execution time) of expanding the gradient descent matrix from 3 points to 20 points, allowing us to project the hardware cost of a full 384-dimensional lock.
**The Execution (Script: `train_micro_corpus_omni.py`):** We execute a heavy-repetition training run (100 epochs) utilizing all 20 derived distances calculated in Phase 1C.

### Phase 12: Precision Omni-Scaffold (The Light Run)
**The Anomaly:** Phase 10 proved that applying heavy kinetic mass (100 epochs) to a 20-point scaffold creates a localized semantic singularity, violently crushing the perimeter distance and neutralizing the subtractive vector calculus.
**The Topological Fix (Precision without Mass):** Because the 20-point scaffold provides absolute multi-dimensional coordinate lock, brute force is no longer required to eliminate rotational spin. We rerun the Omni-Scaffold using light, baseline training parameters to snap the node into the locked geometry without generating the localized gravity that destroys the surrounding manifold.
**The Execution (Script: `train_micro_corpus_omni_light.py`):** We execute a training run utilizing the 20 derived distances from Phase 1C, but revert to 5x multiplier and 10 epochs.

### Phase 13: The Comparative Delta Measurement (The Proof)
We load Model A (Baseline) and all 9 experimental Model B variants (Rigid, Organic, Trajectory, Heavy Repetition, Asymmetric, Parallelogram Scaffold, Omni-Scaffold Heavy, Precision Omni Light, Absolute Omni Cage) and run identical mathematical probes across all ten models to prove the geometric shift and evaluate the biological consolidation hypothesis.

*   **The Teleportation Proof:** Query `Aether-Node`. The coordinate must abandon its random sub-word location ($C_{start}$) and snap into the geometric void in both trained models.
*   **The Anchor Proof:** Rerun the control trajectory ($C_{control}$). The delta must remain near 0.000 across all models, proving surgical precision over blunt trauma.
*   **The Bridge Proof:** Query `Computing + (Quantum Gate - Classical Transistor)`. This forces a navigation path. It tests if the spatial structure between the newly fused `Aether-Node` equivalent nodes forms an interpretable semantic bridge that vector math can traverse.
*   **The Perimeter Shift Proof:** Measure the absolute distance (delta) between `Quantum Gate` and `Classical Bit`. We must prove that forging analogical similarity across the chasm biologically forces the surrounding peripheral concepts into closer geometric density, creating a super-continent cluster.

### Phase 13B: The Margin Analysis (Top-K Expansion)
**The Anomaly:** The Phase 13 Bridge Proof consistently returned the origin node (computing) as the Top-1 result, indicating a collapsed trajectory. However, the scalar distance to computing shifted significantly across models. A Top-1 measurement is a winner-takes-all metric that masks underlying trajectory alignment if the target node sits in the immediate margin (positions 2-5).
**The Execution (Script Re-use):** We do not retrain the models. We re-use the existing `JIT_ATL/Scripts/measure_comparative_delta.py` script, explicitly modifying the GRE retrieval variable from `top_k=1` to `top_k=5` for the Bridge Proof. We sweep the existing models to expose the geometric margin and determine if Aether-Node successfully moved into the immediate directional vicinity.

### Phase 13C: The Margin Analysis (Light Origins)
**The Anomaly:** The Phase 13 Bridge Proof utilizes computing as the origin term. Given its massive gravitational density within the baseline model, it is hypothesized that the origin term itself is artificially anchoring the vector and restricting trajectory magnitude.
**The Topological Fix (Origin Mass Reduction):** Redesign the GRE subtractive calculus using lower-mass classical origin coordinates to test pure navigability without the gravitational anchor of computing.
**The Execution (Script Re-use):** Re-use the existing `measure_comparative_delta.py` script to run two additional Top-5 margin sweeps across all models:

Origin 1: $C = \text{Microchip} + (\text{Quantum Gate} - \text{Classical Transistor})$

Origin 2: $C = \text{Classical Bit} + (\text{Topological Qubit} - \text{Transistor Latch})$

### Phase 14: The N+1 Dimensional Cage (Absolute Omni-Scaffold)
**The Anomaly:** While the 20-point precision scaffold achieved macro-vector alignment, the trajectory lacked the precise magnitude to hit the specific synthetic coordinate. This indicates that rotational degrees of freedom still exist.
**The Topological Fix (The N+1 Law):** By the mathematical law of N-dimensional trilateration, uniquely locking a coordinate requires exactly N+1 constraints. For a 384-dimensional embedding space, we require exactly 385 precise distance constraints. We escalate to a mathematically perfect 385-point derived geometric scaffold.
**The Execution:** 
1. Execute `phase_14_omni_survey_absolute.py` to derive the exact baseline cosine distances to the 385 nearest quantum neighbourhood edge concepts.
2. Execute `train_micro_corpus_absolute.py` using these 385 triplets under "Light" parameters (5x multiplier, 10 epochs) to prevent singularity collapse while enforcing the perfect geometric cage.

### Phase 15: The Semantic Retrieval Proof
**The Anomaly:** We have proven that the internal vector arithmetic functions deterministically via the Bridge Proof. We must now prove that the synthetic coordinate functions in production NLP embeddings.
**The Topological Fix (Natural Language Inference):** Construct a natural language query fully describing the intentional domain: `"the physical switching mechanism of a topological quantum computer"`.
**The Execution (Script: `measure_semantic_retrieval.py`):** Encode the natural language string directly into the resulting model space and sweep the evaluation dictionary. Prove that `[Aether-Node]` organically appears within the nearest Top-5 semantic coordinates without ever explicitly training on this string alignment.

### Phase 16: The Directional Slipstream (Anisotropic Relaxation)
**The Anomaly:** While Semantic Retrieval (Phase 15) successfully locates synthetic nodes using cosine angles, algebraic GRE navigation ($A - B = C$) fails to achieve the correct Euclidean magnitude. An over-constrained $N+1$ spatial cage forces the gradient optimizer to average out distances radially, sacrificing magnitude for stability.
**The Topological Fix (The Onion Architecture):** We abandon uniform spatial caging. Instead, we identify the specific "load-bearing" dimensions of the analogical bridge and apply a boolean gradient mask. By mathematically freezing the orthogonal semantic dimensions, we force the network's tension to relax (slipstream) entirely along the target vector axis, extending the magnitude without causing catastrophic forgetting in the broader map.
**The Execution:** 
1. Execute `phase_16_svd_survey.py` to extract the principal tensor component of the "Classical $\rightarrow$ Quantum" transition and isolate the top load-bearing dimension indices.
2. Execute `train_slipstream_masked.py` using a layered Micro-Corpus, applying `requires_grad = False` to all non-load-bearing dimensions in the PyTorch embedding layer.
---

## IV. ACRONYMS & GLOSSARY
* **GRE:** Geometric Reasoning Engine
* **SLM:** Small Language Model
* **$C_{start}$:** The initial (baseline) coordinate of a concept before training.
* **$C_{new}$:** The new coordinate of a concept after active topological training.
* **$C_{control}$:** The core semantic anchor trajectory used to prove no catastrophic forgetting occurred.
* **$d$:** Distance (usually Cosine Distance or L2 Norm) between two conceptual coordinates.